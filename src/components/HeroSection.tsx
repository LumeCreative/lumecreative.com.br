import React from 'react';
import Fot from "../assets/render.webp"
import MultiLayerParallax from "@/components/MultiLayerParallax"

const HeroSection: React.FC = () => {
  return (
   <MultiLayerParallax/>
  );
};

export default HeroSection;
